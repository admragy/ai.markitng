# 🚀 Brilliox Marketing AI + CRM المتقدم

نظام تسويق رقمي ذكي مع **CRM خطير** + **المحاور الذكي** + **WhatsApp Integration**

## ⭐ المميزات الرئيسية

### 🧠 المحاور الذكي (Smart Conversational AI)
- ✅ فهم النوايا والمشاعر تلقائياً
- ✅ تحليل جودة العملاء (Hot/Warm/Cold)
- ✅ كشف الفرص البيعية فوراً
- ✅ ردود مخصصة وذكية
- ✅ التعلم من المحادثات السابقة
- ✅ تحليل اتجاه المحادثة

### 📱 تكامل WhatsApp Business API
- ✅ إرسال واستقبال رسائل تلقائي
- ✅ ردود ذكية فورية
- ✅ رسائل ترحيب تلقائية
- ✅ تتبع جميع المحادثات
- ✅ إشعارات الفرص الساخنة

### 💼 CRM متقدم
- ✅ قاعدة بيانات SQLite/PostgreSQL
- ✅ تصنيف ذكي للعملاء
- ✅ حساب نقاط تلقائي
- ✅ تتبع التفاعلات
- ✅ إدارة المهام والمتابعة
- ✅ لوحة تحكم تحليلية

### ⚡ أتمتة ذكية شاملة
- ✅ مهام متابعة تلقائية عند إضافة عميل
- ✅ تنبيهات فورية للفرص الساخنة
- ✅ تحديث نقاط العملاء تلقائياً
- ✅ رسائل ترحيب تلقائية
- ✅ توصيات ذكية للإجراءات

### 🎯 Facebook Ads (بدون سجل تجاري)
- ✅ دليل شامل لإنشاء إعلانات
- ✅ طريقة Boosted Posts السهلة
- ✅ لا يحتاج Business Manager
- ✅ الدفع بفيزا عادية

## 🚀 التشغيل السريع

### 1. التثبيت

```bash
# استنساخ المشروع
git clone https://github.com/admragy/brilliox.git
cd brilliox

# تثبيت المكتبات
pip install -r requirements.txt

# نسخ ملف البيئة
cp .env.example .env
```

### 2. إعداد المفاتيح

أضف في `.env`:

```env
# AI (اختر واحد على الأقل)
OPENAI_API_KEY=sk-your-key-here
# أو
GOOGLE_API_KEY=your-google-key

# WhatsApp Business API (اختياري)
WHATSAPP_API_KEY=your-whatsapp-key
WHATSAPP_PHONE_NUMBER_ID=your-phone-id
WHATSAPP_WEBHOOK_TOKEN=brilliox_secure_token
```

### 3. التشغيل

```bash
# التشغيل العادي
python main_crm.py

# أو التشغيل مع الأصلي
python main.py
```

### 4. الوصول

- 📱 التطبيق الرئيسي: `http://localhost:5000`
- 💼 لوحة CRM: `http://localhost:5000/crm`
- 📚 توثيق API: `http://localhost:5000/docs`

## 📊 API Endpoints

### CRM

| Endpoint | Method | الوصف |
|---------|--------|-------|
| `/api/crm/dashboard` | GET | إحصائيات لوحة التحكم |
| `/api/crm/leads` | POST | إنشاء عميل جديد |
| `/api/crm/leads/{id}` | GET | الحصول على بيانات عميل |
| `/api/crm/leads/{id}` | PUT | تحديث بيانات عميل |
| `/api/crm/leads` | GET | البحث في العملاء |
| `/api/crm/leads/{id}/message` | POST | معالجة رسالة (المحاور الذكي) |
| `/api/crm/leads/{id}/send` | POST | إرسال رسالة لعميل |
| `/api/crm/tasks` | GET | الحصول على المهام |

### WhatsApp

| Endpoint | Method | الوصف |
|---------|--------|-------|
| `/api/whatsapp/webhook` | GET | التحقق من webhook |
| `/api/whatsapp/webhook` | POST | استقبال الرسائل |

### التسويق

| Endpoint | Method | الوصف |
|---------|--------|-------|
| `/api/chat` | POST | محادثة مع AI |
| `/api/facebook-ads/guide` | GET | دليل Facebook Ads |
| `/api/health` | GET | فحص الصحة |

## 💡 أمثلة الاستخدام

### إنشاء عميل جديد

```python
import requests

response = requests.post('http://localhost:5000/api/crm/leads', json={
    "name": "أحمد محمد",
    "phone": "+201234567890",
    "email": "ahmed@example.com",
    "company": "شركة التقنية",
    "source": "facebook_ad",
    "notes": "مهتم بخدمات التسويق الرقمي"
})

print(response.json())
# سيتم تلقائياً:
# - حساب النقاط
# - تحديد الجودة (Hot/Warm/Cold)
# - إنشاء مهمة متابعة
# - إرسال رسالة ترحيب على واتساب
```

### معالجة رسالة واردة (المحاور الذكي)

```python
response = requests.post('http://localhost:5000/api/crm/leads/1/message', json={
    "message": "مرحباً، عايز أعرف أسعاركم",
    "channel": "whatsapp"
})

result = response.json()
print(f"الرد: {result['response']}")
print(f"النية: {result['intent']}")
print(f"المشاعر: {result['sentiment']}")
print(f"الجاهزية: {result['readiness']}")
print(f"نقاط الفرصة: {result['opportunity_score']}")

# سيتم تلقائياً:
# - تحليل النية والمشاعر
# - توليد رد مخصص ذكي
# - تحديث نقاط العميل
# - إرسال الرد على واتساب
# - إنشاء مهمة عاجلة إذا كانت فرصة ساخنة
```

### البحث في العملاء

```python
response = requests.get('http://localhost:5000/api/crm/leads', params={
    "status": "new",
    "source": "facebook_ad",
    "search": "أحمد",
    "limit": 20
})

leads = response.json()['leads']
for lead in leads:
    print(f"{lead['name']} - {lead['quality']} - نقاط: {lead['score']}")
```

## 🏗️ بنية المشروع

```
brilliox/
├── app/
│   ├── models/
│   │   └── crm_models.py          # نماذج البيانات
│   └── services/
│       ├── crm_database.py        # قاعدة البيانات
│       ├── smart_conversational_ai.py  # المحاور الذكي
│       ├── whatsapp_service.py    # خدمة واتساب
│       ├── crm_service.py         # CRM API المدمج
│       ├── ai_service_clean.py    # خدمة AI التسويق
│       └── facebook_boost_service.py  # Facebook Ads
├── static/                        # ملفات ثابتة
├── templates/                     # قوالب HTML
├── main.py                        # التطبيق الأصلي
├── main_crm.py                    # التطبيق + CRM
├── requirements.txt               # المكتبات المطلوبة
└── .env.example                   # ملف البيئة المثالي
```

## 🔧 التقنيات المستخدمة

- **Backend:** FastAPI (Python)
- **AI:** OpenAI GPT-4, Google Gemini
- **Database:** SQLite (تطوير), PostgreSQL (إنتاج)
- **WhatsApp:** WhatsApp Business API
- **Frontend:** HTML5, CSS3, JavaScript
- **PWA:** Service Workers, Web App Manifest

## 🎯 حالات الاستخدام

### 1. شركة تسويق رقمي
- إدارة عملاء الحملات الإعلانية
- تتبع جودة العملاء المحتملين
- أتمتة المتابعة والردود

### 2. مكتب مبيعات
- استقبال استفسارات العملاء
- تصنيف تلقائي (Hot/Warm/Cold)
- إشعارات فورية للفرص الساخنة

### 3. خدمة عملاء
- ردود ذكية تلقائية
- تحليل مشاعر العملاء
- تتبع جميع التفاعلات

## 🤝 المساهمة

نرحب بمساهماتك! 

1. Fork المشروع
2. أنشئ فرع جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

## 📝 الترخيص

MIT License - استخدم المشروع بحرية!

## 📞 الدعم

- 🐛 Issues: [GitHub Issues](https://github.com/admragy/brilliox/issues)
- 📧 Email: support@brilliox.ai
- 📚 Docs: `/docs` endpoint

---

**صُنع بـ ❤️ للمسوقين والمبيعات**

النسخة: 6.0.0 ✨  
التاريخ: ديسمبر 2024  
الحالة: ✅ جاهز للإنتاج
